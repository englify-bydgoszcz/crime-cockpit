name: Deploy Crime Cockpit to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Build code-only Pages artifact
        run: |
          rm -rf dist
          mkdir -p dist

          if [ -f site.zip ]; then
            unzip -q site.zip -d dist
          fi

          cp index.html dist/index.html
          cp app.js dist/app.js

          if [ -f styles.css ]; then cp styles.css dist/styles.css; fi
          if [ -f demo-data.js ]; then cp demo-data.js dist/demo-data.js; fi

          # Portrait/media binaries are never deployed from GitHub.
          rm -rf dist/assets
          rm -rf dist/apps-script
          rm -f dist/site.zip
          touch dist/.nojekyll

          test -f dist/index.html
          test -f dist/app.js
          test -f dist/styles.css
          test -f dist/demo-data.js

      - name: Frontend regression smoke
        run: node scripts/frontend-smoke.cjs

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4
